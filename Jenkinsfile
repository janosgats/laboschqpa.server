#!groovy
@Library('pipeline@3.x') _

pipeline {
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    echo "Listing all currently set environment variables..."
                    sh 'env'

                }
            }
        } // stage('Build Docker Image')

    } // stages
}
